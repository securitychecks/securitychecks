/**
 * Schema exports
 *
 * Contains versioning and schema-related utilities.
 * Types are in ../types.ts (pure shapes, no runtime values).
 */

export { ARTIFACT_SCHEMA_VERSION } from './version.js';
